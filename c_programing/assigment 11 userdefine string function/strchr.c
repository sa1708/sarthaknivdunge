#include <stdio.h>
#include <string.h>

int main() {
    char str1[] = "nivdunge";
    char str2[] = "sarthak";

    int result = strcmp(str1, str2);

    if (result == 0) {
        printf("Both strings are equal.\n");
    } else if (result < 0) {
        printf("\n");
    } else {
        printf("\n");
    }

    return 0;
}
